name: Deploy Workflow

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # ✅ AWS Setup (uses your secrets)
      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Debug secrets
        run: |
          echo "Checking if secrets exist..."
          if [ -z "$AWS_ACCESS_KEY_ID" ]; then echo "Missing AWS_ACCESS_KEY_ID"; fi
          if [ -z "$AWS_SECRET_ACCESS_KEY" ]; then echo "Missing AWS_SECRET_ACCESS_KEY"; 
          fi
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      # ✅ Test AWS (optional but important)
      - name: Test AWS Access
        run: aws sts get-caller-identity

      # ✅ Build Docker
      - name: Build Docker Image
        run: docker build -t my-app .

      # ✅ Run container
      - name: Run Container
        run: docker run my-app

      # ✅ REAL DEPLOY (S3 example)
      - name: Upload to S3
        run: aws s3 cp . s3://my-app-bucket --recursive
